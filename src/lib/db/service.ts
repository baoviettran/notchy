// Forwarder — canonical implementation moved to browser/service.ts
export { type QueryResult, type Row, type DatabaseService, uniqueSavepointName, TauriDatabase, createTauriDb } from './browser/service';
