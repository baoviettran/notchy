# v0.1.4 — Database Upgrade Safety + Recovery

**Branch:** `ubuntu-dogfooding-upgrade-safety`
**Specs / Plans:**
- `specs/plans/2026-08-15-ubuntu-dogfooding-upgrade-safety.md`

## Install on Ubuntu

```sh
sudo apt install ./notchy_0.1.4_amd64.deb
```

The deterministic artifact is produced by `pnpm release:dogfood` into `artifacts/0.1.4/`:
`notchy_0.1.4_amd64.deb` plus a `sha256sum -c`-compatible `notchy_0.1.4_amd64.deb.sha256`.

## Schema status

- Current schema: **5**
- Released source schemas that upgrade in place: **3** and **4** (older databases are rejected, never silently touched).

## Upgrade behavior

- Startup now runs a protected sequence: integrity check → schema inspection → mandatory pre-upgrade backup (only when a migration is required) → migration → post-migration verification.
- Pre-upgrade backups are written to **`backups/upgrades/`** below the app-data directory (Tauri `appDataDir()`), with names like `notchy-pre-upgrade-v4-to-v5-0.1.3-2026-08-01T00-00-00-000Z.sqlite`. A routine backup is also available manually from Settings → Backup.
- **0.1.3 → 0.1.4 is a real schema 4 → 5 migration**: the released v0.1.3 caps at schema 4, so upgrading it to v0.1.4 runs the protected migration path — a verified pre-upgrade backup **is** expected and created under `backups/upgrades/`, followed by the migration and post-migration verification.
- **Automatic updates are unsupported**, and **downgrades are unsupported**. A database created by a newer app version (newer schema) is refused, not migrated.

## User-visible changes

### Database recovery screen
If the database cannot be opened safely — corrupt, invalid schema, newer schema, failed pre-upgrade backup, failed migration, or failed post-migration verification — the app shows a full-screen recovery screen with the app version, latest/detected schema, live database path, and the pre-upgrade backup path. Actions: Retry, Restore from pre-upgrade backup (with confirmation), Open backup folder, Copy technical report, Quit.

### Settings → Backup
- Backup health card: app version, schema version, database path, last routine backup timestamp, last upgrade backup path, last upgrade source schema, and any warning.
- Manual "Create backup now" routine backup.
- Restore a compatible older database from a `.sqlite` file (validate → close → replace; incompatible backups are rejected with a localized message, never a raw error).

## Verification

```sh
pnpm test:release-tooling
pnpm test
pnpm check
node -e "const p=require('./package.json'); const t=require('./src-tauri/tauri.conf.json'); if(p.version!==t.version||p.version!=='0.1.4') process.exit(1)"
```

## Manual packaged-build results

Executed 2026-08-16 on the dogfooding workstation.

| Ubuntu version | Source app / schema | Target app / schema | Result | Evidence path |
| --- | --- | --- | --- | --- |
| Ubuntu 24.04.4 LTS (x86_64) | 0.1.3 / schema 4 | 0.1.4 / schema 5 | pass | `~/.local/share/com.notchy.app/backups/upgrades/notchy-pre-upgrade-v4-to-v5-0.1.4-2026-08-16T15-53-39-156Z.sqlite`; live DB `~/.config/com.notchy.app/notchy.db` |

What was verified against the installed 0.1.4 package (after installing the prior 0.1.3 `.deb` from the `v0.1.3` tag and entering non-sensitive sample data):

- **Protected migration (real 4 → 5):** startup detected schema 4, created the verified pre-upgrade backup above under `backups/upgrades/`, ran migration 005, and reached `ready` — `app_meta` shows `last_successful_app_version = 0.1.4`, `last_successful_schema_version = 5`, `last_migrated_from_schema = 4`, `last_upgrade_backup_path = <backup above>`.
- **Data preservation:** 4 accounts, 5 transactions (expense, income, transfer with `transfer_pair_id`), and 1 budget survived. Account balances correct (salary account 12,955,000; savings 2,000,000; both July accounts 1,000,000). Locale `vi`, currency `VND`, quick-add default account intact.
- **Backup/restore round-trip:** a manual `VACUUM INTO` backup was created, an extra transaction added, the backup restored, and the added transaction disappeared while the original rows and transfer direction remained.
- **No-migration claim corrected:** the released `v0.1.3` caps at schema 4, so this checkpoint is a real migration with a pre-upgrade backup — not the earlier "no-migration" assumption.
- **GUI-only exercises** (tray icon actions, `Ctrl+Shift+N` quick-add, reconcile, budget review, form entry) could not be automated on this session (no X automation tool); the tray + global shortcut registration are exercised by the Rust build, and the app reached the dashboard after each launch.
